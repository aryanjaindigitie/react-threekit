import * as products from './products';
import * as catalog from './catalog';
import * as files from './files';
import * as configurations from './configurations';

const http = { products, catalog, files, configurations };

export default http;
