import { hooks, components } from './react';
import * as tools from './tools';

const { ThreekitProvider } = components;

export { hooks, components, ThreekitProvider, tools };

export default {
  hooks,
  components,
  ThreekitProvider,
  tools,
};
